version: '3.8'

services:
  forge-fit:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3018:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    container_name: forge-fit
